# Particle-Collisions-Animation
Creates a short animation of particles colliding, flask is used to upload the animation to an external api (chrome) and get the users input to create a custom animation.
